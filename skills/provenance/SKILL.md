---
name: provenance
description: Preserve governed command provenance across wrappers, traces, and receipts.
---

# Provenance

- Preserve native CLI auth homes.
- Stamp agent/provider/session/run IDs in wrapper environments.
- Use governed shell fallback only when shell grammar is needed.
- Linux cluster mode verifies auditd/execve capture when enabled.
- Windows reports wrapper/user-space provenance only.
- Every autonomous run leaves a trace and receipt.
